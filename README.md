# BanglaGuard: Bengali SMS Spam Detection System

A machine learning-based system designed to classify and detect spam in Bengali SMS messages.

## Project Structure

- `app/`: Backend application logic and API
- `frontend/`: User interface components
- `training/`: Model training and evaluation pipelines
- `data/`: Raw and processed datasets
- `models/`: Saved model artifacts
- `tests/`: Test suites
